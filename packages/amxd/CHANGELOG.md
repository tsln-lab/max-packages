# @tsln/amxd

## 0.1.0

### Minor Changes

- b2f6312: Initial release: decode and encode the `.amxd` container (the `ampf` header, device type, meta flag and `ptch` chunk), with the patcher JSON parsed or kept as text, and `readAmxd`/`writeAmxd` for files.
