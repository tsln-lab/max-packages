---
"@tsln/amxd": minor
---

Initial release: decode and encode the `.amxd` container (the `ampf` header, device type, meta flag and `ptch` chunk), with the patcher JSON parsed or kept as text, and `readAmxd`/`writeAmxd` for files.
